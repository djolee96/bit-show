class Show {
    constructor({ id, name, image, rating, genres, summary }) {
        this.id = id;
        this.title = name;
        this.picture = image
        this.rating = rating.average
        this.genres = genres
        this.summary = summary

    }
}
export default Show